.grid-container.centered
	ul 
		a.tab(ng-click="activeTab = 'Story'" ng-class="{'tabActive': activeTab === 'Story'}") Story
		a.tab(ng-click="activeTab = 'Bundle'" ng-class="{'tabActive': activeTab === 'Bundle'}") Bundle
		.switch-container_animation(ng-switch="activeTab")
			
			//ADD STORY
			.switch-content_animation(ng-switch-when="Story")
				h4.text-center Add Story
				form(method="post" ng-submit="storySubmit()" name="addStoryForm")
					label Story
						input(type="text", ng-model="$parent.storyTitle" placeholder="Enter story title" name="storyTitle" required)
						.validation(ng-if="addStoryForm.storyTitle.$dirty" ng-messages="addStoryForm.storyTitle.$error")
							.message(ng-message="required") Try again, a story title is required
					label Description
						input(type="text", ng-model="$parent.storyDescription" placeholder="Description" name= "storyDescription")

					label Tags
						tags-input(add-on-space="true" ng-model="storyTags" placeholder="Add a tag" name="storyTag")

					button.button-auth.expand(type="submit" ng-disabled="addStoryForm.$invalid") Submit


			//ADD BUNDLE
			.switch-content_animation(ng-switch-when="Bundle")
				h4.text-center Add Bundle
				form(method="post" ng-submit="bundleSubmit()" name="addBundleForm")
					label Title
						input(type="text", ng-model="$parent.title" placeholder="Enter bundle title" name="title" required)
						.validation(ng-if="loginForm.title.$dirty" ng-messages="loginForm.title.$error")
							.message(ng-message="required") Try again, a title is required
					label URL
						input(type="url", ng-model="$parent.url" placeholder="URL" name= "url" required)
						.validation(ng-if="loginForm.url.$dirty" ng-messages="loginForm.url.$error")
							.message(ng-message="required") We do need a url though

					label Tags
						tags-input(add-on-space="true" ng-model="tags" placeholder="Add a tag" name="bundleag")

					button.button-auth.expand(type="submit" ng-disabled="addBundleForm.$invalid") Submit
